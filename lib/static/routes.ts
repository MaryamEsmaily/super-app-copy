const APP_ROUTE_PREFIX = '/home';
const AUTH_ROUTE_PREFIX = '/auth';
const BASE_ROUTE = '/';

const ROUTES = {
  /* Base routes */
  root: `${BASE_ROUTE}`,
  auth: `${AUTH_ROUTE_PREFIX}`,
  home: `${APP_ROUTE_PREFIX}`,
};

export default ROUTES;
